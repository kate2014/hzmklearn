cobra.add({mark_list:'{{~it.data:item:index}}                <div class="mark-item" data-id="{{=item.id}}">                    <a class="delete-icon" href="javascript:;" cb-event="click~deleteMark:{{=item.id}}" title="删除角标">删除</a>                    <div class="mark-img-box">                        <img {{? item.icon_url}}data-src="{{=item.icon_url}}"{{?}} src="http://s1.ve.cn/statics/dot.gif"/>                    </div>                    <div class="info-box">                        <div class="mark-name" title="{{=item.icon_name}}">名称：{{=item.icon_name}}</div>                        <div class="mark-desc" title="{{=item.icon_desc}}">描述：{{=item.icon_desc}}</div>                    </div>                 </div>             {{~}}',corner_list:'{{~it.data:item:index}}                <div class="mark-item check-status" data-id="{{=item.id}}" title="{{=item.icon_name}}" data-thumb="{{=item.icon_url}}">                    <img {{? item.icon_url}}data-src="{{=item.icon_url}}"{{?}} src="http://g.taojae.com/img/x.png"/>                </div>             {{~}}'});